# AI Document Assistant — RAG Starter

This is a minimal starter. For production, replace in-memory storage with Supabase pgvector or Pinecone.

## Run
```bash
npm install
cp .env.example .env.local
npm run dev
```
